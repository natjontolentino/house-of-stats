import { SEED_DEVICE_ID, SEED_LEAGUE_ID } from "@courtstats/shared";

/**
 * Phase 1 hardcodes one league and one device (spec 3) — no pairing flow yet
 * (device pairing is Phase 2, spec 11.3).
 */
export const DEVICE_ID = SEED_DEVICE_ID;
export const LEAGUE_ID = SEED_LEAGUE_ID;
